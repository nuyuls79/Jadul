<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="modelChannel"
            type="net.harimurti.tv.model.Channel" />
        <variable
            name="catId"
            type="int" />
        <variable
            name="chId"
            type="int" />
        <variable
            name="clickListener"
            type="net.harimurti.tv.adapter.ChannelClickListener" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        app:cardBackgroundColor="#2A2A2A"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp"
        android:focusable="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:padding="8dp">

            <!-- Logo dengan jarak bawah -->
            <ImageView
                android:id="@+id/iv_channel_logo"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginBottom="8dp"
                android:scaleType="fitCenter"
                android:adjustViewBounds="true"
                android:padding="4dp"
                android:importantForAccessibility="no"
                tools:src="@drawable/ic_default_logo" />

            <!-- Nama channel dengan background berbeda, hanya ini yang dapat diklik -->
            <TextView
                android:id="@+id/tv_channel_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="8dp"
                android:text="@{modelChannel.name}"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                android:textStyle="bold"
                android:maxLines="2"
                android:ellipsize="end"
                android:gravity="center"
                android:background="@drawable/bg_channel_name"
                android:clickable="true"
                android:focusable="true"
                android:onClick="@{() -> clickListener.onClicked(modelChannel, catId, chId)}"
                tools:text="RCTI" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>
</layout>